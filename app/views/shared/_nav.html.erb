<%= link_to 'Test-Guru', '/' %>
<% if user_signed_in? %>
  <p><%= t('.welcome') %>, <b><%= current_user.first_name %> <%= t('.guru') %></b></p>
  <p><%= link_to t('common.logout'), destroy_user_session_path, method: :delete %></p>
  <p><%= link_to t('common.tests'), tests_path %></p>
<% else %>
  <p>
    <%= link_to t('common.login'), new_user_session_path %>
    <%= link_to t('common.sign_up'), new_user_registration_path %>
  </p>
<% end %>